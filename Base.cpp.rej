diff a/Base.cpp b/Base.cpp	(rejected hunks)
@@ -5,29 +5,30 @@
 using std::cin;
 using std::cout;
 
-double Base::distTo(point alfa, point beta)
+double Base::distTo(point &alfa, point &beta)
 {
 	double dist = sqrt(pow((alfa.x - beta.x), 2) + pow((alfa.y - beta.y), 2));
 	return dist;
 }
 
 
-char Base::create_point()
+void Base::create_point(point p)
 {
-	point p;
-	cout << "Enter X for point: ";
+	cout << "\nEnter X for point: ";
 	cin >> p.x;
-	cout << "\nEnter Y for point: ";
+	cout << "Enter Y for point: ";
 	cin >> p.y;
-	cout << "\nPoint name: ";
-	cin >> p.name;
-	return p.name;
+	cout << "Point created";
 }
 
-Base::Base(double x, double y, char name) : x(x), y(y), name(name) {}
-
-
+Base::Base()
+{
+}
 
 Base::~Base()
 {
 }
+
+
+
+//Base::Base(double x, double y, char name) : x(x), y(y), name(name) {}
\ No newline at end of file
